{% extends 'base.html.twig' %}
{% block title %}Article{% endblock %}

{% block body %}
    <h1>Détail de la carte</h1>
    <div class="card border-primary mb-3 me-2" style="max-width: 20rem;">

                <div class="card-body">
                    <h4 class="card-title">{{card.name}}</h4>
                    <p class="card-text">{{card.description}}</p>
                    <p class="card-text">{{card.price}}</p>
                   {# {% if app.user%}#}
                    <a class="btn btn-outline-warning" href="{{ path('editcard', {id: card.id}) }}">Modifier</a> 
                    <a class="btn btn-outline-danger" href="{{ path('deletecard', {id: card.id}) }}">Supprimer</a>
                   {# {% endif %}#}
                </div>
            </div>
{% endblock %}